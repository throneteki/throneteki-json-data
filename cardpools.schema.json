{
  "additionalProperties": false,
  "definitions": {
    "cardpoolArray": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "original",
          "redesign",
          "draft",
          "kingsmoot",
          "handoftheking"
        ]
      }
    },
    "cardCodeArray": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[0-9]{5}$"
      }
    }
  },
  "type": "array",
  "items": {
    "properties": {
      "code": {
        "type": "string"
      },
      "incompatibleCards": {
        "$ref": "#/definitions/cardCodeArray"
      },
      "incompatibleCardpools": {
        "$ref": "#/definitions/cardpoolArray"
      }
    },
    "required": [
      "code",
      "incompatibleCards",
      "incompatibleCardpools"
    ]
  }
}
